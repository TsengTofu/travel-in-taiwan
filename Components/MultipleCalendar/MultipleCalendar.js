// 研究一下這份文件
// https://react-day-picker.js.org/docs/getting-started
// 試試看寫一下 class component
class MultipleCalendar extends React.Component {
  constructor(props) {
    super(props);
    // 不要在這裡呼叫 this.setState()！ ---->官方警告
    // this.state = { counter: 0 };
    // this.handleClick = this.handleClick.bind(this);
  }
  render() {
    return (
      <>
      </>
    );
  }
}

export default MultipleCalendar;